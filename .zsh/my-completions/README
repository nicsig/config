This folder contains our custom completion functions.

We use them  to get suggestions when pressing Tab  after various shell commands,
such as `xt` or `sr`.

The file containing the code of the completion function for the `foo` command,
must be named `_foo`.

For more info, see:

        https://github.com/zsh-users/zsh/blob/master/Etc/completion-style-guide
        https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org

        https://unix.stackexchange.com/a/366565/232487
        https://github.com/chrisallenlane/cheat/blob/master/cheat/autocompletion/cheat.zsh
