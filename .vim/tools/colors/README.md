For some reason, the `check_colors.vim` script is not installed when I compile Vim.

It should be in:

        $VIMRUNTIME/colors/tool/check_colors.vim

So, for the moment, I've manually installed it here.

